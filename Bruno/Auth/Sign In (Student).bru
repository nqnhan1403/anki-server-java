meta {
  name: Sign In (Student)
  type: http
  seq: 2
}

post {
  url: {{base_url}}/api/auth/signin
  body: json
  auth: inherit
}

body:json {
  {
      "username": "student_1765252880",
      "password": "123456"
  }
}

script:post-response {
  var jsonData = res.getBody();
  bru.setVar("token", jsonData.token);
}

settings {
  encodeUrl: true
  timeout: 0
}
